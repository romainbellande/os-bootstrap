# OS Setup with Ansible

## Install ansible

```shell
sudo apt update &&
sudo apt install software-properties-common &&
sudo apt-add-repository --yes --update ppa:ansible/ansible &&
sudo apt install ansible
```



## available tasks

* yEd graph editor
* visual studio code
* google chrome
* nodejs
* docker-ce
* zsh
* oh-my-zsh
* guake